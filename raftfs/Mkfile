NAME = raftfs

## Build raftfs as server-only
RAFTFS_SERVER ?= 0

.if "${OS}" == "Linux"
HAS_CLIENT = 1
.else
HAS_CLIENT = 0
.endif

.if "${HAS_CLIENT}" == "0" || "${RAFTFS_SERVER}" == "1"
CARGO_BUILD_FLAGS += --no-default-features --features server
.endif

.expand rust
